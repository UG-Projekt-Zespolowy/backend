meta {
  name: CreateProject
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/v1/projects
  body: json
  auth: inherit
}

body:json {
  {
    "name": "New Web Application",
    "description": "Building a modern web app with Spring Boot and React",
    "ownerId": "{{userId}}"
  }
}

vars:pre-request {
  userId: replace-with-userId
}
