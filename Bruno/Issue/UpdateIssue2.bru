meta {
  name: UpdateIssue2
  type: http
  seq: 8
}

patch {
  url: http://localhost:8080/api/v1/issues/{{issueId}}/assign
  body: json
  auth: inherit
}

body:json {
  {
    "assigneeId": "{{assigneeId}}"
  }
}

vars:pre-request {
  issueId: replace-with-uuid
  assigneeId: replace-with-uuid
}
