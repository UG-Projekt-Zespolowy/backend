meta {
  name: UpdateIssue
  type: http
  seq: 7
}

patch {
  url: http://localhost:8080/api/v1/issues/{{issueId}}/status
  body: json
  auth: inherit
}

body:json {
  {
    "status": "DONE"
  }
}

vars:pre-request {
  issueId: replace-with-uuid
}
