meta {
  name: CreateEpic
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/v1/epics
  body: json
  auth: inherit
}

body:json {
  {
    "title": "User Authentication & Authorization",
    "description": "Implement complete user authentication system with JWT tokens, role-based access control, and password reset functionality",
    "projectId": "{{projectId}}"
  }
}

vars:pre-request {
  projectId: replace-with-uuid
}
