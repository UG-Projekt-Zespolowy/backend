meta {
  name: AddUserToProject
  type: http
  seq: 8
}

post {
  url: http://localhost:8080/api/v1/user-projects/projects/{{projectId}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "userId": "{{userId}}",
    "role": "MEMBER"
  }
}

vars:pre-request {
  projectId: replace-with-actual-uuid
  userId: replace-with-actual-uuid
}
